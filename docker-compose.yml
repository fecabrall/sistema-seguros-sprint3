version: '3.8'
services:
  neoroute:
    build: .
    container_name: neoroute_cli
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./exports:/app/exports
    environment:
      - DATABASE_URL=sqlite:///data/seguros.db
